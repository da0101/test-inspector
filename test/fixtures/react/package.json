{
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "react": "latest"
  },
  "devDependencies": {
    "vitest": "latest",
    "@testing-library/react": "latest"
  }
}
